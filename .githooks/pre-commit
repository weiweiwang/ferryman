#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact --verbose --config .gitleaks.toml
fi

python3 scripts/security/secret_scan.py --staged
